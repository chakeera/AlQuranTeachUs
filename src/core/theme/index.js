import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#45503B",
      cream: "#FAEDCD",
      whitewash: "#FEFAE0",
      brown: "#D4A373",
      lightgreen: "#CCD5AE",
    },
    secondary: {
      main: "#CCD5AE",
    },
  },
});

export default theme;
