import { Typography } from '@mui/material';

import React from 'react';

const Sandbox2 = () => {
  return (
    <div>
      <Typography variant="h4">Add Data(String, num) to DB test</Typography>
    </div>
  );
};

export default Sandbox2;
